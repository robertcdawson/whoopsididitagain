# Synthetic fixtures

Every fixture in this directory is fabricated and safe to commit. Never place an export from a
real WHOOP account, Apple Health database, workout history, or clinical record here.

- `whoop/`: representative WHOOP API payloads
- `healthkit/`: normalized HealthKit samples
- `workouts/`: raw workout text
- `expected/`: deterministic expected results
